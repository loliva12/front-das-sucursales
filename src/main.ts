import { bootstrapApplication } from '@angular/platform-browser';
import { SucursalesComponent } from './app/sucursales/sucursales.component';

bootstrapApplication(SucursalesComponent).catch(err => console.error(err));
